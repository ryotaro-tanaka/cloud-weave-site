# GitHub Pages project sites use /repo-name/ as root; user/org pages use base: '/'
site:
  name: Cloud Weave
  site: 'https://ryotaro-tanaka.github.io'
  base: '/cloud-weave-site'
  # Align /base vs /base/ in dev; trailing slash matches common GitHub Pages project URLs
  trailingSlash: true

  googleSiteVerificationId: null

# Default SEO metadata
metadata:
  title:
    default: Cloud Weave
    template: '%s'
  description: 'Bring multiple cloud drives into one Windows workspace. Free open beta download, known limitations, and feedback.'
  robots:
    index: true
    follow: true
  openGraph:
    site_name: Cloud Weave
    images:
      - url: '~/assets/images/cloud-weave-lockup.png'
        width: 1200
        height: 628
    type: website
  twitter:
    handle: ''
    site: ''
    cardType: summary_large_image

i18n:
  language: en
  textDirection: ltr

apps:
  blog:
    isEnabled: false
    postsPerPage: 6

    post:
      isEnabled: true
      permalink: '/%slug%' # Variables: %slug%, %year%, %month%, %day%, %hour%, %minute%, %second%, %category%
      robots:
        index: true

    list:
      isEnabled: true
      pathname: 'blog' # Blog main path, you can change this to "articles" (/articles)
      robots:
        index: true

    category:
      isEnabled: true
      pathname: 'category' # Category main path /category/some-category, you can change this to "group" (/group/some-category)
      robots:
        index: true

    tag:
      isEnabled: true
      pathname: 'tag' # Tag main path /tag/some-tag, you can change this to "topics" (/topics/some-category)
      robots:
        index: false

    isRelatedPostsEnabled: true
    relatedPostsCount: 4

analytics:
  vendors:
    googleAnalytics:
      id: null # or "G-XXXXXXXXXX"

ui:
  theme: 'system' # Values: "system" | "light" | "dark" | "light:only" | "dark:only"
